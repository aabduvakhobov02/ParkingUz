import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EndUserScreenHeader = () => {
  return (
    <View>
      <Text>EndUserScreenHeader</Text>
    </View>
  );
};

export default EndUserScreenHeader;

const styles = StyleSheet.create({});
