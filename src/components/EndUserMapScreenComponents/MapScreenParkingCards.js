import React from 'react';
import {StyleSheet, FlatList} from 'react-native';

import MapScreenParkingCard from './MapScreenParkingCard';

const MapScreenParkingCards = ({
  parkings,
  hours,
  setActiveModal,
  setIsActive,
}) => {
  return <></>;
};

export default MapScreenParkingCards;

const styles = StyleSheet.create({});
